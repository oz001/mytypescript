export { };
let numbers: number[] = [1, 2, 3, 4];
console.log({ array: numbers });

let numbers2: Array<number> = [1, 2, 3];
console.log({ array: numbers2 });

let strings2: Array<string> = ['tokyo', 'kyoto', 'osaka'];

let strings: string[] = ['TypeString', 'Javascript', 'CoffeeScript'];

let two_dimension: number[][] = [
  [10, 20],
  [30, 50],
];

let gene_array: (string | number | boolean)[] = [1, true, 'japan'];

console.log(gene_array);

